# REST API (Future Ready)

*Currently, Order Shield relies on standard WordPress AJAX endpoints for its admin dashboard.*

In future versions, a fully authenticated REST API (`/wp-json/order-shield/v1/`) will be introduced to allow external integrations (e.g., syncing blocklists across multiple WooCommerce sites).